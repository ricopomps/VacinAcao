import CalendarDay from "./CalendarDay";

import { render } from "@testing-library/react";

describe("CalendarDay", () => {
  it("Renderiza CalendarDay", () => {
    render(<CalendarDay />);
  });
});
